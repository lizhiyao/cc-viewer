# CC-Viewer

نظام مراقبة طلبات Claude Code، يلتقط ويعرض بشكل مرئي جميع طلبات واستجابات API الخاصة بـ Claude Code في الوقت الفعلي (النص الأصلي، بدون اختصار). يساعد المطورين على مراقبة السياق الخاص بهم لمراجعة المشكلات واستكشافها أثناء عملية Vibe Coding.
يوفر أحدث إصدار من CC-Viewer أيضًا حلول نشر الخادم للبرمجة عبر الويب، بالإضافة إلى أدوات البرمجة على الأجهزة المحمولة. نرحب بتطبيقه في مشاريعكم، وسيتم فتح المزيد من ميزات الإضافات مستقبلاً مع دعم النشر السحابي.

[English](../README.md) | [简体中文](./README.zh.md) | [繁體中文](./README.zh-TW.md) | [한국어](./README.ko.md) | [日本語](./README.ja.md) | [Deutsch](./README.de.md) | [Español](./README.es.md) | [Français](./README.fr.md) | [Italiano](./README.it.md) | [Dansk](./README.da.md) | [Polski](./README.pl.md) | [Русский](./README.ru.md) | العربية | [Norsk](./README.no.md) | [Português (Brasil)](./README.pt-BR.md) | [ไทย](./README.th.md) | [Türkçe](./README.tr.md) | [Українська](./README.uk.md)

## طريقة الاستخدام

### التثبيت

```bash
npm install -g cc-viewer --registry=https://registry.npmjs.org
```

### وضع المراقبة (في هذا الوضع، عند تشغيل claude أو claude --dangerously-skip-permissions سيتم تشغيل عملية تسجيل تلقائيًا لتسجيل رسائل الطلبات)

```bash
ccv
```

### وضع البرمجة

== claude

```bash
ccv -c
```

== claude --dangerously-skip-permissions

```bash
ccv -d
```

بعد تشغيل وضع البرمجة، سيتم فتح صفحة الويب تلقائيًا.

يمكنك استخدام claude مباشرة من صفحة الويب، مع إمكانية عرض رسائل الطلبات الكاملة ومراجعة تغييرات الكود.

والأكثر إثارة، يمكنك حتى البرمجة من جهازك المحمول!

يكتشف هذا الأمر تلقائيًا طريقة تثبيت Claude Code المحلية (NPM أو Native Install) ويتكيف معها.

- **تثبيت NPM**: يحقن تلقائيًا سكريبت الاعتراض في ملف `cli.js` الخاص بـ Claude Code.
- **التثبيت الأصلي**: يكتشف تلقائيًا ملف `claude` الثنائي، ويهيئ وكيلًا شفافًا محليًا، ويضبط Zsh Shell Hook لإعادة توجيه حركة المرور تلقائيًا.
- يُوصى في هذا المشروع باستخدام Claude Code المثبت عبر npm.

### تجاوز الإعدادات (Configuration Override)

إذا كنت بحاجة إلى استخدام نقطة نهاية API مخصصة (مثل وكيل المؤسسة)، فقط قم بالتهيئة في `~/.claude/settings.json` أو عيّن متغير البيئة `ANTHROPIC_BASE_URL`. سيتعرف `ccv` عليه تلقائيًا ويعيد توجيه الطلبات بشكل صحيح.

### الوضع الصامت (Silent Mode)

بشكل افتراضي، يعمل `ccv` في الوضع الصامت عند تغليف `claude`، مما يضمن بقاء مخرجات الطرفية نظيفة ومتسقة مع التجربة الأصلية. يتم التقاط جميع السجلات في الخلفية ويمكن عرضها عبر `http://localhost:7008`.

بعد اكتمال التهيئة، استخدم أمر `claude` كالمعتاد. قم بزيارة `http://localhost:7008` لعرض واجهة المراقبة.

### استكشاف الأخطاء وإصلاحها (Troubleshooting)

إذا واجهت مشكلة في التشغيل، هناك حل نهائي لاستكشاف الأخطاء:
الخطوة الأولى: افتح Claude Code في أي مجلد؛
الخطوة الثانية: أعطِ Claude Code التعليمات التالية:
```
لقد قمت بتثبيت حزمة npm المسماة cc-viewer، لكن بعد تنفيذ ccv لا يعمل بشكل صحيح. افحص cli.js و findcc.js الخاصين بـ cc-viewer، وقم بالتكيف مع طريقة نشر Claude Code المحلية وفقًا للبيئة المحددة. حاول حصر نطاق التعديلات في findcc.js.
```
السماح لـ Claude Code بفحص الأخطاء بنفسه أكثر فعالية من استشارة أي شخص أو قراءة أي وثائق!

بعد اكتمال التعليمات أعلاه، سيتم تحديث findcc.js. إذا كان مشروعك يحتاج بشكل متكرر إلى النشر المحلي، أو إذا كان الكود المتفرع يحتاج إلى حل مشاكل التثبيت بشكل متكرر، فاحتفظ بهذا الملف. في المرة القادمة يمكنك نسخ الملف مباشرة. في المرحلة الحالية، العديد من المشاريع والشركات لا تنشر Claude Code على Mac، بل تستضيفه على الخادم، لذلك فصل المؤلف ملف findcc.js لتسهيل متابعة تحديثات الكود المصدري لـ cc-viewer.

### إلغاء التثبيت

```bash
ccv --uninstall
```

### التحقق من الإصدار

```bash
ccv -v
```

## الميزات

### مراقبة الطلبات (وضع النص الأصلي)
<img width="1500" height="720" alt="image" src="https://github.com/user-attachments/assets/519dd496-68bd-4e76-84d7-2a3d14ae3f61" />

- التقاط جميع طلبات API الصادرة من Claude Code في الوقت الفعلي، مع ضمان أنها النص الأصلي وليست سجلات مختصرة (هذا مهم جدًا!!!)
- التعرف التلقائي على طلبات Main Agent و Sub Agent ووسمها (الأنواع الفرعية: Plan، Search، Bash)
- طلبات MainAgent تدعم Body Diff JSON، عرض مطوي للفروقات مع طلب MainAgent السابق (عرض الحقول المتغيرة/الجديدة فقط)
- عرض إحصائيات استخدام Token مضمنة لكل طلب (Token المدخلة/المخرجة، إنشاء/قراءة ذاكرة التخزين المؤقت، نسبة الإصابة)
- متوافق مع Claude Code Router (CCR) وسيناريوهات الوكيل الأخرى — مطابقة احتياطية للطلبات عبر أنماط مسار API

### وضع المحادثة

انقر على زر «وضع المحادثة» في الزاوية العلوية اليمنى لتحليل سجل المحادثة الكامل لـ Main Agent كواجهة دردشة:
<img width="1500" height="730" alt="image" src="https://github.com/user-attachments/assets/c973f142-748b-403f-b2b7-31a5d81e33e6" />

- لا يدعم حاليًا عرض Agent Team
- رسائل المستخدم محاذاة لليمين (فقاعات زرقاء)، ردود Main Agent محاذاة لليسار (فقاعات داكنة)
- كتل `thinking` مطوية افتراضيًا، معروضة بتنسيق Markdown، انقر للتوسيع وعرض عملية التفكير؛ تدعم الترجمة بنقرة واحدة (الميزة لا تزال غير مستقرة)
- رسائل اختيار المستخدم (AskUserQuestion) معروضة بتنسيق سؤال وجواب
- مزامنة ثنائية الاتجاه: عند التبديل إلى وضع المحادثة يتم تحديد الموقع تلقائيًا للمحادثة المقابلة للطلب المحدد؛ عند العودة إلى وضع النص الأصلي يتم تحديد الموقع تلقائيًا للطلب المحدد
- لوحة الإعدادات: يمكن تبديل حالة الطي الافتراضية لنتائج الأدوات وكتل التفكير
- تصفح المحادثات على الهاتف: في وضع CLI على الهاتف، انقر على زر «تصفح المحادثات» في الشريط العلوي لعرض واجهة محادثة للقراءة فقط، لتصفح سجل المحادثة الكامل على الهاتف

### وضع البرمجة

بعد التشغيل باستخدام ccv -c أو ccv -d يمكنك رؤية:
<img width="1500" height="725" alt="image" src="https://github.com/user-attachments/assets/a64a381e-5a68-430c-b594-6d57dc01f4d3" />

يمكنك عرض فروقات الكود مباشرة بعد الانتهاء من التحرير:
<img width="1500" height="728" alt="image" src="https://github.com/user-attachments/assets/2a4acdaa-fc5f-4dc0-9e5f-f3273f0849b2" />

على الرغم من أنه يمكنك فتح الملفات والبرمجة يدويًا، إلا أنه لا يُنصح بذلك، فتلك برمجة تقليدية!

### البرمجة على الأجهزة المحمولة

يمكنك حتى مسح رمز QR للبرمجة على جهازك المحمول:
<img width="3018" height="1460" alt="image" src="https://github.com/user-attachments/assets/8debf48e-daec-420c-b37a-609f8b81cd20" />

على جهازك المحمول يمكنك رؤية:
<img width="1700" height="790" alt="image" src="https://github.com/user-attachments/assets/da3e519f-ff66-4cd2-81d1-f4e131215f6c" />

يحقق تصوراتك عن البرمجة على الأجهزة المحمولة.

### أدوات الإحصائيات

لوحة «إحصائيات البيانات» المنبثقة في منطقة الرأس:
<img width="1500" height="729" alt="image" src="https://github.com/user-attachments/assets/b23f9a81-fc3d-4937-9700-e70d84e4e5ce" />

- عرض عدد cache creation/read ونسبة إصابة ذاكرة التخزين المؤقت
- إحصائيات إعادة بناء ذاكرة التخزين المؤقت: مجمعة حسب السبب (TTL، تغيير system/tools/model، اقتطاع/تعديل الرسائل، تغيير المفتاح) مع عدد المرات وعدد cache_creation tokens
- إحصائيات استخدام الأدوات: عرض تكرار استدعاء كل أداة مرتبة حسب عدد الاستدعاءات
- إحصائيات استخدام Skill: عرض تكرار استدعاء كل Skill مرتبة حسب عدد الاستدعاءات
- أيقونة المساعدة المفاهيمية (?): انقر لعرض الوثائق المدمجة لـ MainAgent و CacheRebuild والأدوات المختلفة

### إدارة السجلات

عبر القائمة المنسدلة CC-Viewer في أعلى اليسار:
<img width="1200" height="672" alt="image" src="https://github.com/user-attachments/assets/8cf24f5b-9450-4790-b781-0cd074cd3b39" />

- استيراد السجلات المحلية: تصفح ملفات السجلات التاريخية، مجمعة حسب المشروع، تفتح في نافذة جديدة
- تحميل ملف JSONL محلي: اختيار ملف `.jsonl` محلي مباشرة للتحميل والعرض (يدعم حتى 500 ميجابايت)
- حفظ السجل الحالي باسم: تنزيل ملف سجل JSONL الحالي للمراقبة
- دمج السجلات: دمج عدة ملفات سجلات JSONL في جلسة واحدة للتحليل الموحد
- عرض Prompt المستخدم: استخراج وعرض جميع مدخلات المستخدم، يدعم ثلاثة أوضاع عرض — وضع النص الأصلي (المحتوى الخام)، وضع السياق (وسوم النظام قابلة للطي)، وضع النص (نص عادي فقط)؛ أوامر الشرطة المائلة (`/model`، `/context`، إلخ) تُعرض كإدخالات مستقلة؛ الوسوم المتعلقة بالأوامر تُخفى تلقائيًا من محتوى Prompt
- تصدير Prompt إلى TXT: تصدير Prompt المستخدم (نص عادي، بدون وسوم النظام) إلى ملف `.txt` محلي

### دعم متعدد اللغات

يدعم CC-Viewer 18 لغة، ويتبدل تلقائيًا بناءً على إعدادات لغة النظام:

简体中文 | English | 繁體中文 | 한국어 | Deutsch | Español | Français | Italiano | Dansk | 日本語 | Polski | Русский | العربية | Norsk | Português (Brasil) | ไทย | Türkçe | Українська

### التحديث التلقائي

يتحقق CC-Viewer تلقائيًا من التحديثات عند بدء التشغيل (مرة واحدة كل 4 ساعات كحد أقصى). ضمن نفس الإصدار الرئيسي (مثل 1.x.x → 1.y.z) يتم التحديث تلقائيًا ويسري عند إعادة التشغيل التالية. التغييرات عبر الإصدارات الرئيسية تعرض إشعارًا فقط.

يتبع التحديث التلقائي إعدادات Claude Code العامة في `~/.claude/settings.json`. إذا كان Claude Code قد عطّل التحديثات التلقائية (`autoUpdates: false`)، فسيتخطى CC-Viewer التحديث التلقائي أيضًا.

## License

MIT
